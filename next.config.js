
module.exports = {
    /* crie sua variável deambiente para uso local, aqui. para uso em produção, no server, vai depender de onde você está fazendo o deploy. pesquise sobre : variáveis de embiente em produção. */
  }